# В деве API доступен через Vite proxy (/api → localhost:3000), поэтому
# VITE_API_URL не нужен. Задайте его только если хотите ходить на бэкенд
# напрямую (например, другой хост/порт).
# VITE_API_URL=http://localhost:3000
