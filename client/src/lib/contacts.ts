/**
 * Контакты — одним местом на весь сайт: они встречаются в мете первого экрана,
 * в шапке страниц кейсов и в резюме (и попадают в cv.pdf). Пока телеграм жил
 * строкой в разметке, в шапке он вообще не был ссылкой — «> написать» рисовался
 * <div>'ом и не кликался.
 */

export const TELEGRAM_HANDLE = "@okonnaya";
export const TELEGRAM_URL = "https://t.me/okonnaya";
export const EMAIL = "carina.rama@ya.ru";
export const EMAIL_URL = `mailto:${EMAIL}`;

/* ─────────────────────────────────────────────────────────────────────────
   Адрес портфолио — с протоколом и без слеша на конце.

   Отсюда он попадает и на страницу резюме, и в cv.pdf: pdf печатается из той
   же страницы на каждой сборке (client/scripts/cv-pdf.mjs). Это важно именно
   для pdf — файл пересылают отдельно от ссылки, и без адреса сайта из него не
   попасть в кейсы. Если строку опустошить, ссылка просто не выводится.

   При переезде на другой домен поправьте и og:url с og:image в index.html —
   они прописаны абсолютными и сами отсюда не подтягиваются.
   ───────────────────────────────────────────────────────────────────────── */
export const SITE_URL = "https://okonnaya.com";

/** как адрес показывается в тексте: без протокола, «karina.design» */
export const SITE_LABEL = SITE_URL.replace(/^https?:\/\//, "").replace(/\/$/, "");

/** подпись ссылки «написать» — тоже одна на все места */
export const WRITE_LABEL = "> написать";
